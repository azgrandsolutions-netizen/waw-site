// src/app/[locale]/page.tsx
export default function LocaleHome() {
  return (
    <main style={{ padding: 24 }}>
      <h1>OK — /[locale]/page.tsx</h1>
      <p>Dacă vezi asta pe /ro, pagina e randată corect.</p>
    </main>
  );
}
